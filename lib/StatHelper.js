import $ from 'sprint-js';

class StatHelper {
  
  constructor({ cubeBuffer }) {
    this.cubeBuffer = cubeBuffer;
  }
  
  removeCube(cube) {
    console.log(cube);
  }
  
}

export default StatHelper;